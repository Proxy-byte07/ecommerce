# Server Configuration
PORT=5001
NODE_ENV=development

# MongoDB Connection
# Set a MongoDB Atlas / local URI, or leave commented/empty to use the automatic built-in in-memory database
# MONGODB_URI=mongodb://localhost:27017/ecommerce_db

# JWT Configuration
JWT_SECRET=super_secret_jwt_key_for_ecommerce_app_development_2026
JWT_EXPIRES_IN=7d
